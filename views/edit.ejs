<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      background: #dceefd; /* light blue Wix-style background */
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 40px 20px;
    }
    .layout-root {
      max-width: 800px;
      width: 100%;
      background: #e6f0ff; /* lighter compatible card background */
      padding: 30px 40px;
      border-radius: 14px;
      box-shadow: 0 12px 24px rgba(0, 82, 204, 0.12);
      box-sizing: border-box;
    }
    .blog-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
    }
    .header-brand {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .blog-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: #0a2d6d;
      margin: 0;
    }
    .logo-icon svg {
      width: 36px;
      height: 36px;
      color: #0a2d6d;
    }
    .header-actions .btn-icon-outline {
      background-color: transparent;
      border: 1.5px solid #0a2d6d;
      color: #0a2d6d;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 18px;
      font-size: 15px;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease, color 0.3s ease;
      text-decoration: none;
      user-select: none;
    }
    .header-actions .btn-icon-outline:hover {
      background-color: #0a2d6d;
      color: #e6f0ff;
    }
    .header-actions .btn-icon-outline svg {
      width: 20px;
      height: 20px;
      stroke: currentColor;
      stroke-width: 2;
    }

    h1.posts-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: #0a2d6d;
      margin-bottom: 24px;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .form-group {
      display: flex;
      flex-direction: column;
    }
    .form-control {
      padding: 14px 16px;
      font-size: 16px;
      font-weight: 500;
      border-radius: 10px;
      border: 2px solid #a8c4ff;
      background-color: #f0f7ff;
      color: #0a2d6d;
      font-family: 'Inter', sans-serif;
      transition: border-color 0.3s ease;
      box-sizing: border-box;
    }
    .form-control:focus {
      border-color: #0a2d6d;
      outline: none;
      background-color: #e6f0ff;
    }
    textarea.form-control {
      min-height: 140px;
      resize: vertical;
    }
    button.btn-icon-outline,
    a.btn-icon-outline {
      background-color: transparent;
      border: 2px solid #0a2d6d;
      color: #0a2d6d;
      font-weight: 600;
      padding: 14px 22px;
      font-size: 16px;
      border-radius: 10px;
      cursor: pointer;
      user-select: none;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      transition: background-color 0.3s ease, color 0.3s ease;
      text-decoration: none;
      box-sizing: border-box;
    }
    button.btn-icon-outline:hover,
    a.btn-icon-outline:hover {
      background-color: #0a2d6d;
      color: #e6f0ff;
    }
    button.btn-icon-outline svg,
    a.btn-icon-outline svg {
      width: 22px;
      height: 22px;
      stroke: currentColor;
      stroke-width: 2;
    }
    a.btn-icon-outline.ml-4 {
      margin-left: 20px;
    }
  </style>
</head>
<body>
  <div class="layout-root">
    <header class="blog-header">
      <div class="header-brand">
        <div class="logo-icon">
          <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M4 4H17.3334V17.3334H30.6666V30.6666H44V44H4V4Z" fill="currentColor" />
          </svg>
        </div>
        <h2 class="blog-title">My Blog</h2>
      </div>
      <div class="header-actions">
        <a href="/" class="btn-icon-outline" title="Back to Posts">
          <!-- Back icon -->
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
          Back to Posts
        </a>
      </div>
    </header>

    <h1 class="posts-title">Edit Post</h1>
    <form method="POST" action="/edit/<%= post.id %>">
      <div class="form-group">
        <input type="text" name="title" value="<%= post.title %>" class="form-control" required />
      </div>
      <div class="form-group">
        <textarea name="content" class="form-control" required><%= post.content %></textarea>
      </div>
      <div>
        <button type="submit" class="btn-icon-outline">
          <!-- Save icon -->
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
          </svg>
          Save Changes
        </button>
        <a href="/" class="btn-icon-outline ml-4" title="Cancel">
          <!-- Cancel icon -->
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
          Cancel
        </a>
      </div>
    </form>
  </div>
</body>
</html>
