<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Tech Blog</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
  :root {
    --primary: #3b82f6;
    --primary-hover: #2563eb;
    --primary-light: #dbeafe;
    --accent: #8b5cf6;
    --accent-hover: #7c3aed;
    --background: #0f0f23;
    --surface: #1a1a3a;
    --surface-light: #252545;
    --text-primary: #ffffff;
    --text-secondary: #a8a8b3;
    --text-muted: #6c6c89;
    --border: #2a2a4a;
    --success: #10b981;
    --warning: #f59e0b;
    --error: #ef4444;
    --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --gradient-accent: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    --gradient-surface: linear-gradient(135deg, #1a1a3a 0%, #252545 100%);
    --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.3);
    --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
    --shadow-lg: 0 8px 25px rgba(0, 0, 0, 0.5);
    --shadow-glow: 0 0 20px rgba(59, 130, 246, 0.3);
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--background);
    color: var(--text-primary);
    line-height: 1.7;
    font-size: 16px;
    overflow-x: hidden;
  }

  /* Animated background */
  body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
      radial-gradient(circle at 20% 50%, rgba(120, 119, 198, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 40% 80%, rgba(139, 92, 246, 0.1) 0%, transparent 50%);
    z-index: -1;
    animation: backgroundPulse 8s ease-in-out infinite alternate;
  }

  @keyframes backgroundPulse {
    0% { opacity: 0.5; }
    100% { opacity: 0.8; }
  }

  header {
    background: rgba(26, 26, 58, 0.95);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
    padding: 1.25rem 2rem;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: var(--shadow-md);
  }

  .logo-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--text-primary);
    font-weight: 800;
    font-size: 1.5rem;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .logo h2 {
    font-size: 1.5rem;
    font-weight: 800;
    margin: 0;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .logo div {
    width: 32px !important;
    height: 32px !important;
    background: var(--gradient-primary);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-glow);
    animation: logoGlow 3s ease-in-out infinite alternate;
  }

  @keyframes logoGlow {
    0% { box-shadow: 0 0 10px rgba(59, 130, 246, 0.3); }
    100% { box-shadow: 0 0 20px rgba(59, 130, 246, 0.6); }
  }

  nav {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  nav a {
    text-decoration: none;
    font-size: 0.95rem;
    color: var(--text-secondary);
    font-weight: 500;
    padding: 0.75rem 1.25rem;
    border-radius: 12px;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  nav a::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transition: left 0.5s;
  }

  nav a:hover::before {
    left: 100%;
  }

  nav a:hover {
    background: var(--gradient-primary);
    color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  nav a[href="/new"] {
    background: var(--gradient-accent);
    color: white;
    font-weight: 600;
    box-shadow: var(--shadow-md);
  }

  nav a[href="/new"]:hover {
    transform: translateY(-2px) scale(1.05);
    box-shadow: var(--shadow-lg);
  }

  .nav-icon {
    width: 18px;
    height: 18px;
    stroke: currentColor;
    stroke-width: 2;
  }

  .hero {
    padding: 4rem 2rem;
    background: var(--gradient-surface);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
    animation: heroFloat 6s ease-in-out infinite alternate;
  }

  @keyframes heroFloat {
    0% { transform: translate(0, 0) rotate(0deg); }
    100% { transform: translate(-20px, -20px) rotate(5deg); }
  }

  .hero-inner {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-banner h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    margin-bottom: 1rem;
    color: var(--text-primary);
    line-height: 1.1;
    background: linear-gradient(135deg, #ffffff 0%, #a8a8b3 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: titleSlide 1s ease-out;
  }

  @keyframes titleSlide {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-banner h2 {
    font-size: clamp(1.1rem, 2.5vw, 1.5rem);
    font-weight: 400;
    color: var(--text-secondary);
    max-width: 700px;
    line-height: 1.6;
    animation: subtitleSlide 1s ease-out 0.2s both;
  }

  @keyframes subtitleSlide {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .recent-posts-title {
    margin-top: 4rem;
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--text-primary);
    text-align: center;
    position: relative;
    padding-bottom: 1rem;
  }

  .recent-posts-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 4px;
    background: var(--gradient-primary);
    border-radius: 2px;
  }

  .recent-post-box {
    background: var(--surface);
    border-radius: 20px;
    padding: 2.5rem;
    margin-top: 2rem;
    box-shadow: var(--shadow-lg);
    border: 1px solid var(--border);
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    animation: postSlide 0.6s ease-out;
  }

  @keyframes postSlide {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .recent-post-box::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--gradient-primary);
  }

  .recent-post-box:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.6);
    border-color: var(--primary);
  }

  .recent-post-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .recent-post-content {
    color: var(--text-secondary);
    font-size: 1.1rem;
    line-height: 1.7;
    margin-top: 0.5rem;
    margin-bottom: 2rem;
  }

  .post-actions {
    margin-top: 1rem;
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .btn-icon {
    background: var(--surface-light);
    border: 1px solid var(--border);
    cursor: pointer;
    padding: 0.75rem;
    border-radius: 12px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .btn-icon:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .btn-edit:hover {
    background: var(--primary);
    border-color: var(--primary);
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
  }

  .btn-delete:hover {
    background: var(--error);
    border-color: var(--error);
    box-shadow: 0 4px 15px rgba(239, 68, 68, 0.4);
  }

  .icon-sm {
    width: 20px;
    height: 20px;
    stroke: var(--text-secondary);
    transition: stroke 0.3s ease;
  }

  .btn-icon:hover .icon-sm {
    stroke: white;
  }

  .comments-section {
    border-top: 1px solid var(--border);
    padding-top: 2rem;
  }

  .comments-section h4 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .comments-section h4::before {
    content: '💬';
    font-size: 1.5rem;
  }

  .comment {
    background: var(--surface-light);
    border-radius: 12px;
    padding: 1.5rem;
    margin-top: 1rem;
    border-left: 4px solid var(--accent);
    transition: all 0.3s ease;
  }

  .comment:hover {
    background: rgba(139, 92, 246, 0.1);
    transform: translateX(4px);
  }

  .comment p {
    color: var(--text-secondary);
    margin: 0 0 1rem 0;
    font-size: 1rem;
  }

  .comment-actions {
    margin-top: 0.5rem;
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .comment-actions form {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    flex: 1;
  }

  .comment-actions .form-control {
    background: var(--surface);
    border: 1px solid var(--border);
    color: var(--text-primary);
    padding: 0.75rem 1rem;
    border-radius: 8px;
    font-family: inherit;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    flex: 1;
  }

  .comment-actions .form-control:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
  }

  .comment-form {
    background: var(--surface-light);
    border-radius: 16px;
    padding: 1.5rem;
    margin-top: 1rem;
    border: 1px solid var(--border);
    display: flex;
    gap: 0.5rem;
    align-items: center;
    transition: all 0.3s ease;
  }

  .comment-form:focus-within {
    border-color: var(--primary);
    box-shadow: var(--shadow-glow);
  }

  .comment-form textarea {
    resize: none;
    width: 100%;
    height: 48px;
    border: none;
    padding: 1rem;
    font-family: inherit;
    font-size: 1rem;
    background: var(--surface);
    color: var(--text-primary);
    border-radius: 12px;
    outline: none;
    transition: all 0.3s ease;
  }

  .comment-form textarea::placeholder {
    color: var(--text-muted);
  }

  .comment-form textarea:focus {
    background: var(--background);
  }

  .comment-form button {
    background: var(--gradient-primary);
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
    font-family: inherit;
  }

  .comment-form button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .comment-form button svg {
    width: 20px;
    height: 20px;
    stroke: currentColor;
    transition: stroke 0.2s ease;
  }

  .inline-form {
    display: inline;
  }

  @keyframes highlight {
    0% { 
      background: rgba(59, 130, 246, 0.2);
      transform: scale(1.02);
    }
    100% { 
      background: transparent;
      transform: scale(1);
    }
  }

  .highlighted {
    animation: highlight 2s ease-out;
  }

  /* Scrollbar styling */
  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }

  ::-webkit-scrollbar-track {
    background: var(--surface);
  }

  ::-webkit-scrollbar-thumb {
    background: var(--primary);
    border-radius: 4px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: var(--primary-hover);
  }

  /* Mobile responsiveness */
  @media (max-width: 768px) {
    header {
      padding: 1rem;
    }
    
    .logo-container {
      flex-direction: column;
      gap: 1rem;
    }

    nav {
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.5rem;
      margin-top: 0.5rem;
    }

    nav a {
      padding: 0.5rem 1rem;
      font-size: 0.9rem;
    }

    .hero {
      padding: 2rem 1rem;
    }

    .hero-banner h1 {
      font-size: 2rem;
    }

    .hero-banner h2 {
      font-size: 1rem;
    }

    .recent-post-box {
      padding: 1.5rem;
      margin-top: 1.5rem;
    }

    .post-actions {
      gap: 0.5rem;
    }

    .comment-form {
      flex-direction: column;
      align-items: stretch;
    }

    .comment-actions {
      flex-direction: column;
      gap: 0.5rem;
    }

    .comment-actions form {
      flex-direction: column;
    }

    .recent-posts-title {
      font-size: 2rem;
      margin-top: 2rem;
    }
  }

  /* Dark mode enhancements */
  @media (prefers-color-scheme: dark) {
    :root {
      --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.5);
      --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.6);
      --shadow-lg: 0 8px 25px rgba(0, 0, 0, 0.7);
    }
  }

  /* Smooth transitions for all interactive elements */
  * {
    transition: color 0.3s ease, background-color 0.3s ease, border-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
  }
</style>

</head>
<body>
  <header>
    <div class="logo-container">
      <div class="logo">
        <div style="width: 16px; height: 16px;">
          <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M39.5563 34.1455V13.8546..." fill="currentColor"></path>
            <path d="M10.4485 13.8519C10.4749 13.9271..." fill="currentColor"></path>
          </svg>
        </div>
        <h2 style="font-size: 18px; font-weight: bold; margin: 0;">Tech Blog</h2>
      </div>
      <nav>
        <a href="/">Home</a>
        <a href="/blog">Articles</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
        <a href="/new">
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
          </svg>
          New Blog
        </a>
      </nav>
    </div>
  </header>

  <div class="hero">
    <div class="hero-inner">
      <div class="hero-banner">
        <div>
          <h1>The Future of Work: Embracing Remote Collaboration</h1>
          <h2>Explore the latest trends in remote work, tools for effective collaboration, and strategies for maintaining productivity in a distributed team.</h2>
        </div>
      </div>

      <h2 class="recent-posts-title" id="posts-top">Recent Posts</h2>

      <% posts.forEach(post => { %>
        <div class="recent-post-box" id="post-<%= post.id %>">
          <h3 class="recent-post-title"><%= post.title %></h3>
          <p class="recent-post-content"><%= post.content %></p>

          <div class="post-actions">
            <a href="/edit/<%= post.id %>" class="btn-icon btn-edit" title="Edit Post">
              <svg class="icon-sm" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                      d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
              </svg>
            </a>

            <form method="POST" action="/delete/<%= post.id %>" class="inline-form">
              <button type="submit" class="btn-icon btn-delete" title="Delete Post">
                <svg class="icon-sm" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                        d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                </svg>
              </button>
            </form>
          </div>

          <div class="comments-section">
            <h4>Comments</h4>
            <% if (post.comments && post.comments.length > 0) { %>
              <% post.comments.forEach(comment => { %>
                <div class="comment">
                  <p><%= comment.content %></p>
                  <div class="comment-actions">
                    <form method="POST" action="/comment/edit/<%= comment.id %>" class="flex gap-2">
                      <input type="text" name="content" value="<%= comment.content %>" class="form-control" required>
                      <button type="submit" class="btn-icon btn-edit" title="Update Comment">
                        <svg class="icon-sm" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                                d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
                        </svg>
                      </button>
                    </form>

                    <form method="POST" action="/comment/delete/<%= comment.id %>">
                      <button type="submit" class="btn-icon btn-delete" title="Delete Comment">
                        <svg class="icon-sm" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                        </svg>
                      </button>
                    </form>
                  </div>
                </div>
              <% }) %>
            <% } %>

            <form method="POST" action="/comments/<%= post.id %>" class="comment-form">
              <textarea name="content" placeholder="Write your comment..." required></textarea>
              <button type="submit" class="btn btn-primary">Add Comment</button>
            </form>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      if(window.location.hash) {
        const element = document.querySelector(window.location.hash);
        if(element) {
          element.scrollIntoView({ behavior: 'smooth' });
          element.style.animation = 'highlight 1s ease-out';
        }
      }
    });
  </script>
</body>
</html>